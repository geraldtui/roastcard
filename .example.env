# Resend API key (server-only secret). Leave unset to run in local "mock send"
# mode, where emails are logged to the server console instead of sent.
RESEND_API_KEY=

# Fixed, verified-domain From address. Must be on a domain verified in Resend.
FROM_EMAIL=cards@roastcard.geraldtui.com
